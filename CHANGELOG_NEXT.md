## v1.10.0 — xxyy 浮窗模式 + 聚合范围切换

xxyy 主面板和「我的 聚合」面板各自新增两个独立切换按钮：

### 🪟 浮窗模式（📌/🪟）

- 默认 📌 = 嵌入式（在监控卡片内部，跟随原卡布局）
- 点一下变 🪟 = **浮窗模式**：右上角 fixed 定位、可拖拽、位置记忆
- 浮窗状态下拖头部移动、再点 📌 收回原位置
- 主面板和我的面板独立配置，可以一个嵌入一个浮窗
- 浮窗位置分别保存在 `posMain` / `posMy`

### 🌐 聚合范围（🌐/📍）

控制本面板聚合检测的钱包范围：

- 默认 🌐 = **全页面**：所有监控卡的钱包都参与聚合（保留 v1.9.0 多面板行为）
- 点一下变 📍 = **仅本面板**：只聚合该面板所在卡片当前展示的钱包

实现细节：
- 每张卡片维护一个白名单 Set（持续累积可见钱包名 + 已知地址）
- scope='panel' 时聚合检测加白名单过滤
- 用户滚动卡片 → 白名单越来越完整 → 范围越来越准

典型用法：
- 主面板 🌐 全页面聚合追大热点
- 我的面板 📍 + 浮窗 🪟，挂在屏幕角落只聚合「我的」卡片里的小范围共振

gmgn 单面板架构不受影响。
